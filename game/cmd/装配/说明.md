# 装配组件

装配组件负责当前七个槽位和六套配装的玩家交互。它不提供通用武库或物品详情查询。

```text
装配
装备 物品编号
卸下 槽位
配装
配装 0-5
```

装备命令从物品定义自动取得唯一槽位。替换、卸下和配装切换统一调用 `PersistedLoadoutService`，库存位置
与装配状态在同一个 SQLite 工作单元中提交。当前配装发生装备或卸下后自动保存，不提供额外保存命令。
